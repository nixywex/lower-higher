{
  "name": "lower-higher",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "lint": "eslint ."
  },
  "lint-staged": {
    "client/**/*.{ts,tsx}": [
      "./client/node_modules/.bin/eslint --fix --config ./client/eslint.config.js",
      "prettier --write"
    ],
    "server/**/*.{ts,js}": [
      "prettier --write"
    ],
    "*.{json,md}": [
      "prettier --write"
    ]
  },
  "dependencies": {
    "react-router-dom": "^7.16.0"
  }
}
